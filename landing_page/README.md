# Exlser Landing Page

This folder is reserved for the future React landing page.

Planned target:

```text
exlser.com      -> React landing page
exlser.com/app  -> optional Flutter web app
```

The React project should be scaffolded only after the Flutter app migration to
`flutter_app/` is committed and the release workflows are verified.
