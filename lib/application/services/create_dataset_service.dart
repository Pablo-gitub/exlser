/// Handles the post-confirmation commit flow:
/// 6. Create dataset
/// 7. Create dataset table(s)
/// 8. Register columns
/// 9. Build dynamic SQL table(s)
/// 10. Insert parsed rows
///
/// This service is invoked only after schema confirmation from the UI.
class CreateDatasetService {
  const CreateDatasetService();

  Future<void> createDataset() async {
    // TODO:
    // 6. Create dataset
    // 7. Create dataset table(s)
    // 8. Register columns
    // 9. Build dynamic SQL table(s)
    // 10. Insert parsed rows
    throw UnimplementedError();
  }
}